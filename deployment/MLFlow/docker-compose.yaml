version: '3'
services:
  mlflow:
    image: ghrc.io/mlflow/mlflow:latest
    ports:
      - "5001:5000"
    command: mlflow server --backend-store-uri sqlite:///mlflow.db --default-artifact-root ./mlruns --host 0.0.0.0 --port 5001
    container_name: mlflow-tracking-server